# =============================================
# Check My Stocks 환경 설정
# 이 파일을 복사하여 .env.local로 저장하세요
# 예: cp .env.example .env.local
# =============================================

# ---------------------------------------------
# 관리자 인증 설정 (필수)
# ---------------------------------------------
# 관리자 아이디
VITE_ADMIN_ID=your_admin_id

# 관리자 비밀번호 해시 (SHA-256)
# 해시 생성: node -e "console.log(require('crypto').createHash('sha256').update('your_password').digest('hex'))"
VITE_ADMIN_PW_HASH=your_password_sha256_hash

# JWT 서명용 시크릿 키 (임의의 긴 문자열 권장)
VITE_JWT_SECRET=your-super-secret-jwt-key-change-this

# ---------------------------------------------
# GitHub Personal Access Token (선택)
# ---------------------------------------------
# 분석 시작 버튼으로 GitHub Actions를 자동 트리거하려면 설정 필요
#
# PAT 생성 방법:
# 1. GitHub → Settings → Developer settings → Fine-grained tokens
# 2. Generate new token
# 3. Repository access: Only select repositories → check_my_stocks 선택
# 4. Permissions:
#    - Actions: Read and write
# 5. Generate token → 복사하여 아래에 붙여넣기

VITE_GITHUB_PAT=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
